# BEACON server dependencies — pinned versions (CLAUDE.md principle)
# Python 3.11+ required (3.13 verified — sqlalchemy>=2.0.31 needed for 3.13)

# Core web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic>=2.7
python-multipart==0.0.17

# WebSocket support
websockets==12.0

# Database & ORM
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10  # Postgres driver — no-op for SQLite, required for the
# Azure Postgres Flexible Server deploy target (ADR-0004). Missing entirely
# before this: the container built but crashed on startup against a real
# Postgres DATABASE_URL (verified locally, see docs/BUILD-LOG.md).

# Date/time handling
python-dateutil==2.8.2

# Environment variables
python-dotenv==1.0.1

# Testing
pytest==8.0.0
pytest-asyncio==0.23.6
httpx==0.27.2

# Routing (G2 — team-orienteering route planner)
ortools==9.12.4544  # G2 route planner (h3 dropped — existing hex_id fixtures
# aren't valid H3 cell indices; centroid derived from real Claim lat/lng instead)

# Data loading (scripts/load_claims.py — real Gradhack_Insure_Data.xlsx ingest)
pandas==2.2.3
openpyxl==3.1.5

# Computer vision preprocessing (src/vision/preprocess.py — CLAHE for low light,
# frame sampling ahead of the Roboflow plate/weapon calls). Missing entirely
# before this: fresh installs of requirements.txt could not import src.vision
# at all (ModuleNotFoundError: cv2), breaking pytest collection.
opencv-python-headless==4.10.0.84
numpy==2.2.1

# Type checking (development)
mypy==1.8.0
